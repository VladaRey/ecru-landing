# План реализации интернационализации лендинга Ecru

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Отдавать лендинг на английском и украинском, каждый по своему URL, без фреймворка и без единой рантайм-зависимости.

**Architecture:** `src/index.html` — шаблон с `{{ключами}}`; `i18n/<lang>.json` — словари; `build.js` на Node без зависимостей делает текстовую подстановку и пишет `dist/index.html` (en) и `dist/uk/index.html`. Чистые функции рендера экспортируются и покрыты тестами через встроенный `node --test`; файловый слой проверяется на фикстурах.

**Tech Stack:** Node ≥20 (CommonJS, `node:test`, `node:assert`), GitHub Actions, GitHub Pages. Ноль npm-зависимостей, `package.json` не заводим.

**Spec:** `docs/superpowers/specs/2026-08-25-i18n-design.md`

## Global Constraints

- **Ноль зависимостей.** Ни `package.json`, ни `node_modules`, ни CDN. Только стандартная библиотека Node.
- **Ноль внешних запросов у собранной страницы.** Шрифты, картинки, стили — всё локальное. Это не аккуратность: страница обещает, что приложение не ходит в сеть.
- **Язык по умолчанию — `en`, лежит в корне `dist/`.** Остальные языки — в `dist/<lang>/`. Существующие ссылки на `https://vladarey.github.io/ecru-landing/` не должны сломаться.
- **Все пути в разметке относительные,** через `{{@base}}`. Корне-абсолютные пути ломают и `/uk/`, и открытие через `file://`.
- **Сборка падает с ненулевым кодом** при отсутствующем или лишнем ключе в любом словаре.
- **Комментарии в коде — украинские,** как в существующих `waitlist.js` и `.github/workflows/`. README — английский.
- **Работаем в ветке `i18n`.** Пуш в `main` немедленно деплоит сайт; промежуточные коммиты деплоить нельзя.
- **Instrument Serif не трогаем.** Кириллица добавляется отдельным семейством через `unicode-range`; страницы `en`, `pl`, `de` не меняются ни на пиксель.
- Site URL: `https://vladarey.github.io/ecru-landing/`
- Endonyms языков: `en` → `English`, `uk` → `Українська`, `pl` → `Polski`, `de` → `Deutsch`. Живут константой в `build.js`, в словари не попадают: самоназвание языка не переводится.

---

### Task 0: Ветка

**Files:**
- Нет

- [ ] **Step 1: Убедиться, что дерево чистое**

Run: `git status --porcelain`
Expected: пустой вывод

- [ ] **Step 2: Создать ветку**

```bash
git checkout -b i18n
```

- [ ] **Step 3: Проверить версию Node**

Run: `node --version`
Expected: `v20.` или выше. `node --test` на директории есть только с 20; на 18 придётся перечислять файлы вручную.

---

### Task 1: Ядро рендера — подстановка и валидация ключей

Чистые функции, никакого доступа к диску. Это то, что удерживает четыре языка от расхождения, поэтому оно появляется первым и с тестами.

**Files:**
- Create: `build.js`
- Test: `tests/build.test.js`

**Interfaces:**
- Consumes: ничего
- Produces:
  - `keysIn(template) -> Set<string>` — все `{{ключи}}` шаблона
  - `render(template, values) -> string` — подстановка; бросает `Error`, если для ключа нет значения
  - `checkKeys(template, strings, lang) -> string[]` — список проблем; пустой массив значит «всё сходится»

- [ ] **Step 1: Написать падающие тесты**

Создать `tests/build.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const { keysIn, render, checkKeys } = require('../build.js');

test('keysIn збирає всі ключі шаблона', () => {
  const found = keysIn('<h1>{{hero.title}}</h1><p>{{hero.lede}}</p>');
  assert.deepStrictEqual([...found].sort(), ['hero.lede', 'hero.title']);
});

test('keysIn не плутає службові ключі зі звичайними', () => {
  const found = keysIn('<html lang="{{@lang}}"><img src="{{@base}}a.jpg" alt="{{a.alt}}">');
  assert.deepStrictEqual([...found].sort(), ['@base', '@lang', 'a.alt']);
});

test('render підставляє значення дослівно, разом з розміткою', () => {
  const out = render('<p>{{k}}</p>', { k: 'tells you <em>why</em> — in one sentence' });
  assert.strictEqual(out, '<p>tells you <em>why</em> — in one sentence</p>');
});

test('render підставляє один ключ у всіх його входженнях', () => {
  const out = render('{{cta}}|{{cta}}', { cta: 'Get early access' });
  assert.strictEqual(out, 'Get early access|Get early access');
});

test('render падає, коли значення немає', () => {
  assert.throws(() => render('<p>{{missing}}</p>', {}), /missing/);
});

test('checkKeys мовчить, коли шаблон і словник збігаються', () => {
  const problems = checkKeys('<p>{{a}}{{b}}</p>', { a: '1', b: '2' }, 'en');
  assert.deepStrictEqual(problems, []);
});

test('checkKeys ловить відсутній переклад', () => {
  const problems = checkKeys('<p>{{a}}{{b}}</p>', { a: '1' }, 'uk');
  assert.strictEqual(problems.length, 1);
  assert.match(problems[0], /uk/);
  assert.match(problems[0], /b/);
});

test('checkKeys ловить зайвий ключ у словнику', () => {
  const problems = checkKeys('<p>{{a}}</p>', { a: '1', ghost: '2' }, 'uk');
  assert.strictEqual(problems.length, 1);
  assert.match(problems[0], /ghost/);
});

test('checkKeys не вимагає службових ключів від словника', () => {
  const problems = checkKeys('<html lang="{{@lang}}">{{a}}', { a: '1' }, 'en');
  assert.deepStrictEqual(problems, []);
});
```

- [ ] **Step 2: Запустить тесты и убедиться, что они падают**

Run: `node --test tests/`
Expected: FAIL — `Cannot find module '../build.js'`

- [ ] **Step 3: Написать минимальную реализацию**

Создать `build.js`:

```js
'use strict';

// Збірка не парсить HTML. Вона робить одну текстову заміну {{ключів}},
// тому вкладена розмітка всередині рядка — не окремий випадок: значення
// зберігається у словнику разом з тегами і підставляється цілком.

const KEY_RE = /\{\{([\w.@-]+)\}\}/g;

function keysIn(template) {
  return new Set(Array.from(template.matchAll(KEY_RE), (m) => m[1]));
}

function render(template, values) {
  return template.replace(KEY_RE, (whole, key) => {
    if (!(key in values)) throw new Error(`немає значення для {{${key}}}`);
    return values[key];
  });
}

// Порожній масив означає «сходиться». Ключ, що лишився в словнику без
// вживання, — така сама помилка, як відсутній переклад: він означає, що
// шаблон змінили, а словники за ним не пішли.
function checkKeys(template, strings, lang) {
  const used = keysIn(template);
  const missing = [...used].filter((k) => !k.startsWith('@') && !(k in strings));
  const unused = Object.keys(strings).filter((k) => !used.has(k));

  const problems = [];
  if (missing.length) problems.push(`${lang}: немає перекладу для ${missing.join(', ')}`);
  if (unused.length) problems.push(`${lang}: у словнику зайві ключі ${unused.join(', ')}`);
  return problems;
}

module.exports = { keysIn, render, checkKeys };
```

- [ ] **Step 4: Запустить тесты и убедиться, что они проходят**

Run: `node --test tests/`
Expected: PASS, 8 tests

- [ ] **Step 5: Коммит**

```bash
git add build.js tests/build.test.js
git commit -m "feat: ядро збірки — підстановка ключів і перевірка словників"
```

---

### Task 2: Служебные `@`-ключи

Значения, которые заполняет сборка, а не переводчик. По-прежнему чистые функции.

**Files:**
- Modify: `build.js`
- Test: `tests/build.test.js`

**Interfaces:**
- Consumes: `render`, `checkKeys` из Task 1
- Produces:
  - `LANGS = ['en', 'uk']`, `DEFAULT_LANG = 'en'`, `SITE = 'https://vladarey.github.io/ecru-landing/'`, `LANG_NAMES`
  - `basePrefix(lang) -> string` — `''` для корня, `'../'` для языковой папки
  - `pageUrl(lang) -> string` — абсолютный URL страницы
  - `alternates(langs) -> string` — блок `<link rel="alternate">`, включая `x-default`
  - `langSwitch(current, langs) -> string` — разметка переключателя
  - `metaFor(lang, langs) -> {'@lang', '@base', '@canonical', '@alternates', '@langswitch'}`

- [ ] **Step 1: Написать падающие тесты**

Дописать в `tests/build.test.js`:

```js
const { basePrefix, pageUrl, alternates, langSwitch, metaFor } = require('../build.js');

test('basePrefix порожній у корені і піднімає на рівень у мовній теці', () => {
  assert.strictEqual(basePrefix('en'), '');
  assert.strictEqual(basePrefix('uk'), '../');
});

test('pageUrl кладе мову за умовчанням у корінь', () => {
  assert.strictEqual(pageUrl('en'), 'https://vladarey.github.io/ecru-landing/');
  assert.strictEqual(pageUrl('uk'), 'https://vladarey.github.io/ecru-landing/uk/');
});

test('alternates перелічує всі мови, включно з собою, і додає x-default', () => {
  const out = alternates(['en', 'uk']);
  assert.match(out, /hreflang="en" href="https:\/\/vladarey\.github\.io\/ecru-landing\/"/);
  assert.match(out, /hreflang="uk" href="https:\/\/vladarey\.github\.io\/ecru-landing\/uk\/"/);
  assert.match(out, /hreflang="x-default" href="https:\/\/vladarey\.github\.io\/ecru-landing\/"/);
  assert.strictEqual(out.match(/<link/g).length, 3);
});

test('langSwitch робить поточну мову не посиланням', () => {
  const out = langSwitch('uk', ['en', 'uk']);
  assert.match(out, /<span aria-current="page">Українська<\/span>/);
  assert.doesNotMatch(out, /<a[^>]*>Українська</);
});

test('langSwitch веде з мовної теки на корінь через ../', () => {
  assert.match(langSwitch('uk', ['en', 'uk']), /href="\.\.\/"[^>]*>English/);
});

test('langSwitch веде з кореня у мовну теку без ../', () => {
  assert.match(langSwitch('en', ['en', 'uk']), /href="uk\/"[^>]*>Українська/);
});

test('langSwitch ніколи не видає порожній href', () => {
  for (const current of ['en', 'uk']) {
    assert.doesNotMatch(langSwitch(current, ['en', 'uk']), /href=""/);
  }
});

test('metaFor віддає повний набір службових ключів', () => {
  const meta = metaFor('uk', ['en', 'uk']);
  assert.strictEqual(meta['@lang'], 'uk');
  assert.strictEqual(meta['@base'], '../');
  assert.strictEqual(meta['@canonical'], 'https://vladarey.github.io/ecru-landing/uk/');
  assert.ok(meta['@alternates'].includes('x-default'));
  assert.ok(meta['@langswitch'].includes('aria-current'));
});
```

- [ ] **Step 2: Запустить тесты и убедиться, что они падают**

Run: `node --test tests/`
Expected: FAIL — `basePrefix is not a function`

- [ ] **Step 3: Написать реализацию**

Дописать в `build.js` перед `module.exports`:

```js
// Додати мову — дописати код сюди і покласти поруч i18n/<code>.json.
// Тека, hreflang і перемикач виводяться звідси, руками нічого не дублюється.
const LANGS = ['en', 'uk'];
const DEFAULT_LANG = 'en';
const SITE = 'https://vladarey.github.io/ecru-landing/';

// Самоназви. У словники не потрапляють: назва мови не перекладається —
// на англійській сторінці українська так само «Українська».
const LANG_NAMES = { en: 'English', uk: 'Українська', pl: 'Polski', de: 'Deutsch' };

// Шляхи в розмітці відносні, бо сайт віддається з /ecru-landing/, а не з
// кореня домену. Побічний виграш: dist/uk/index.html відкривається
// подвійним кліком через file:// і виглядає правильно.
function basePrefix(lang) {
  return lang === DEFAULT_LANG ? '' : '../';
}

function pageUrl(lang) {
  return lang === DEFAULT_LANG ? SITE : `${SITE}${lang}/`;
}

function alternates(langs) {
  const links = langs.map(
    (l) => `<link rel="alternate" hreflang="${l}" href="${pageUrl(l)}" />`
  );
  links.push(`<link rel="alternate" hreflang="x-default" href="${pageUrl(DEFAULT_LANG)}" />`);
  return links.join('\n    ');
}

function langSwitch(current, langs) {
  const base = basePrefix(current);
  return langs
    .map((l) => {
      if (l === current) return `<span aria-current="page">${LANG_NAMES[l]}</span>`;
      const href = `${base}${l === DEFAULT_LANG ? '' : `${l}/`}`;
      return `<a href="${href}">${LANG_NAMES[l]}</a>`;
    })
    .join('\n            ');
}

function metaFor(lang, langs) {
  return {
    '@lang': lang,
    '@base': basePrefix(lang),
    '@canonical': pageUrl(lang),
    '@alternates': alternates(langs),
    '@langswitch': langSwitch(lang, langs),
  };
}
```

и расширить экспорт:

```js
module.exports = {
  keysIn, render, checkKeys,
  basePrefix, pageUrl, alternates, langSwitch, metaFor,
  LANGS, DEFAULT_LANG, SITE, LANG_NAMES,
};
```

- [ ] **Step 4: Запустить тесты и убедиться, что они проходят**

Run: `node --test tests/`
Expected: PASS, 16 tests

Примечание про `langSwitch` из корня: `href` для `en` там был бы пустым, но `en` в корне всегда текущий и отдаётся как `<span>`. Тест `ніколи не видає порожній href` держит это свойство на случай, если язык по умолчанию когда-нибудь переедет.

- [ ] **Step 5: Коммит**

```bash
git add build.js tests/build.test.js
git commit -m "feat: службові ключі збірки — base, canonical, hreflang, перемикач мов"
```

---

### Task 3: Файловый слой и CLI

Чтение шаблона и словарей, запись `dist/`, копирование статики. Проверяется на фикстурах — настоящий шаблон появится только в Task 4.

**Files:**
- Modify: `build.js`
- Create: `tests/build.fs.test.js`
- Create: `tests/fixtures/src/index.html`
- Create: `tests/fixtures/i18n/en.json`
- Create: `tests/fixtures/i18n/uk.json`
- Create: `tests/fixtures/style.css`
- Create: `tests/fixtures/.nojekyll`
- Create: `tests/fixtures/assets/shots/x.txt`

**Interfaces:**
- Consumes: `render`, `checkKeys`, `metaFor` из Task 1–2
- Produces: `build({root, outDir, langs, defaultLang}) -> {written: string[]}`; бросает `Error` со всеми проблемами словарей сразу

- [ ] **Step 1: Создать фикстуры**

```bash
mkdir -p tests/fixtures/src tests/fixtures/i18n tests/fixtures/assets/shots
touch tests/fixtures/.nojekyll
echo 'body { color: red }' > tests/fixtures/style.css
echo 'shot' > tests/fixtures/assets/shots/x.txt
```

`tests/fixtures/src/index.html`:

```html
<!doctype html>
<html lang="{{@lang}}">
  <head>
    <link rel="canonical" href="{{@canonical}}" />
    {{@alternates}}
    <link rel="stylesheet" href="{{@base}}style.css" />
  </head>
  <body>
    <nav>{{@langswitch}}</nav>
    <h1>{{hero.title}}</h1>
    <img src="{{@base}}assets/shots/x.txt" alt="{{hero.alt}}" />
  </body>
</html>
```

`tests/fixtures/i18n/en.json`:

```json
{
  "hero.title": "Your whole wardrobe",
  "hero.alt": "A screenshot"
}
```

`tests/fixtures/i18n/uk.json`:

```json
{
  "hero.title": "Уся ваша гардеробна",
  "hero.alt": "Знімок екрана"
}
```

- [ ] **Step 2: Написать падающие тесты**

Создать `tests/build.fs.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');
const { build } = require('../build.js');

const FIXTURES = path.join(__dirname, 'fixtures');

function run(overrides = {}) {
  const outDir = fs.mkdtempSync(path.join(os.tmpdir(), 'ecru-build-'));
  const result = build({
    root: FIXTURES,
    outDir,
    langs: ['en', 'uk'],
    defaultLang: 'en',
    ...overrides,
  });
  return { outDir, result };
}

test('мова за умовчанням лягає в корінь, решта — у свої теки', () => {
  const { outDir } = run();
  assert.ok(fs.existsSync(path.join(outDir, 'index.html')));
  assert.ok(fs.existsSync(path.join(outDir, 'uk', 'index.html')));
});

test('у зібраних сторінках не лишається жодного {{', () => {
  const { outDir } = run();
  for (const rel of ['index.html', 'uk/index.html']) {
    const html = fs.readFileSync(path.join(outDir, rel), 'utf8');
    assert.doesNotMatch(html, /\{\{/, `${rel} містить незамінений ключ`);
  }
});

test('кожна сторінка отримує свою мову й свій текст', () => {
  const { outDir } = run();
  const en = fs.readFileSync(path.join(outDir, 'index.html'), 'utf8');
  const uk = fs.readFileSync(path.join(outDir, 'uk', 'index.html'), 'utf8');
  assert.match(en, /<html lang="en">/);
  assert.match(uk, /<html lang="uk">/);
  assert.match(en, /Your whole wardrobe/);
  assert.match(uk, /Уся ваша гардеробна/);
});

test('шляхи в мовній теці піднімаються на рівень', () => {
  const { outDir } = run();
  const uk = fs.readFileSync(path.join(outDir, 'uk', 'index.html'), 'utf8');
  assert.match(uk, /href="\.\.\/style\.css"/);
  assert.match(uk, /src="\.\.\/assets\/shots\/x\.txt"/);
});

test('статика копіюється один раз у корінь dist', () => {
  const { outDir } = run();
  assert.ok(fs.existsSync(path.join(outDir, 'style.css')));
  assert.ok(fs.existsSync(path.join(outDir, 'assets', 'shots', 'x.txt')));
  assert.ok(fs.existsSync(path.join(outDir, '.nojekyll')));
  assert.ok(!fs.existsSync(path.join(outDir, 'uk', 'style.css')));
});

test('повторна збірка не лишає файлів від попередньої', () => {
  const { outDir } = run();
  fs.writeFileSync(path.join(outDir, 'stale.html'), 'old');
  build({ root: FIXTURES, outDir, langs: ['en', 'uk'], defaultLang: 'en' });
  assert.ok(!fs.existsSync(path.join(outDir, 'stale.html')));
});

test('build падає, коли словника мови немає', () => {
  assert.throws(() => run({ langs: ['en', 'uk', 'pl'] }), /pl/);
});

test('node build.js виходить з ненульовим кодом, коли словники розійшлися', () => {
  const root = fs.mkdtempSync(path.join(os.tmpdir(), 'ecru-root-'));
  fs.cpSync(FIXTURES, root, { recursive: true });
  fs.writeFileSync(path.join(root, 'build.js'), fs.readFileSync(path.join(__dirname, '..', 'build.js')));
  fs.writeFileSync(path.join(root, 'i18n', 'uk.json'), JSON.stringify({ 'hero.title': 'Тільки заголовок' }));
  const { status, stderr } = require('node:child_process').spawnSync(
    'node', ['build.js'], { cwd: root, encoding: 'utf8' }
  );
  assert.notStrictEqual(status, 0, 'збірка мала впасти');
  assert.match(stderr, /hero\.alt/);
});

test('build називає всі проблеми словників одразу, а не першу-ліпшу', () => {
  const outDir = fs.mkdtempSync(path.join(os.tmpdir(), 'ecru-build-'));
  const root = fs.mkdtempSync(path.join(os.tmpdir(), 'ecru-root-'));
  fs.cpSync(FIXTURES, root, { recursive: true });
  fs.writeFileSync(
    path.join(root, 'i18n', 'uk.json'),
    JSON.stringify({ 'hero.title': 'Тільки заголовок', ghost: 'зайвий' })
  );
  assert.throws(
    () => build({ root, outDir, langs: ['en', 'uk'], defaultLang: 'en' }),
    (err) => /hero\.alt/.test(err.message) && /ghost/.test(err.message)
  );
});
```

- [ ] **Step 3: Запустить тесты и убедиться, что они падают**

Run: `node --test tests/`
Expected: FAIL — `build is not a function`

- [ ] **Step 4: Написать реализацию**

Дописать в `build.js` перед `module.exports`:

```js
const fs = require('node:fs');
const path = require('node:path');

// Копіюється один раз у корінь dist. Шляхи до шрифтів усередині style.css
// рахуються від самого CSS-файла, тож він однаково працює для обох сторінок
// і множити його по мовних теках не треба.
const STATIC = ['style.css', 'waitlist.js', 'assets', '.nojekyll'];

function readStrings(root, lang) {
  const file = path.join(root, 'i18n', `${lang}.json`);
  if (!fs.existsSync(file)) throw new Error(`немає словника ${lang}: ${file}`);
  try {
    return JSON.parse(fs.readFileSync(file, 'utf8'));
  } catch (err) {
    throw new Error(`словник ${lang} не розбирається: ${err.message}`);
  }
}

function build({ root, outDir, langs = LANGS, defaultLang = DEFAULT_LANG }) {
  const template = fs.readFileSync(path.join(root, 'src', 'index.html'), 'utf8');

  // Спочатку звіряємо всі словники і аж тоді щось пишемо: краще впасти зі
  // списком усіх розбіжностей, ніж лагодити їх по одній.
  const dictionaries = new Map(langs.map((l) => [l, readStrings(root, l)]));
  const problems = langs.flatMap((l) => checkKeys(template, dictionaries.get(l), l));
  if (problems.length) throw new Error(`словники розійшлися з шаблоном:\n  ${problems.join('\n  ')}`);

  fs.rmSync(outDir, { recursive: true, force: true });
  fs.mkdirSync(outDir, { recursive: true });

  const written = [];
  for (const lang of langs) {
    const values = { ...dictionaries.get(lang), ...metaFor(lang, langs) };
    const dir = lang === defaultLang ? outDir : path.join(outDir, lang);
    fs.mkdirSync(dir, { recursive: true });
    const file = path.join(dir, 'index.html');
    fs.writeFileSync(file, render(template, values));
    written.push(file);
  }

  for (const entry of STATIC) {
    const from = path.join(root, entry);
    if (fs.existsSync(from)) {
      fs.cpSync(from, path.join(outDir, entry), { recursive: true });
    }
  }

  return { written };
}

if (require.main === module) {
  try {
    const root = __dirname;
    const { written } = build({ root, outDir: path.join(root, 'dist') });
    console.log(`зібрано: ${written.length} сторінок`);
  } catch (err) {
    console.error(err.message);
    process.exit(1);
  }
}
```

Добавить `build` в экспорт.

**Внимание:** `metaFor(lang, langs)` внутри `build` использует модульные `DEFAULT_LANG`/`SITE`, а не параметр `defaultLang`. Для этого проекта они всегда совпадают; параметр существует только ради читаемости тестов. Не расширять его смысл, не проверив `basePrefix` и `pageUrl`.

- [ ] **Step 5: Запустить тесты и убедиться, что они проходят**

Run: `node --test tests/`
Expected: PASS, 25 tests

- [ ] **Step 6: Коммит**

```bash
git add build.js tests/
git commit -m "feat: файловий шар збірки — dist, копіювання статики, падіння на розбіжностях"
```

---

### Task 4: Конвертация страницы в шаблон и `i18n/en.json`

Самая объёмная задача и единственная, где легко потерять текст. Поэтому её приёмка — не «на глаз», а точное совпадение с текущей страницей.

**Files:**
- Create: `src/index.html` (из текущего `index.html`)
- Create: `i18n/en.json`
- Delete: `index.html`
- Modify: `.gitignore`
- Create: `tests/output.test.js`

**Interfaces:**
- Consumes: `build` из Task 3
- Produces: `src/index.html` с ~130 ключами; `i18n/en.json` с теми же ключами

- [ ] **Step 1: Завести dist в .gitignore**

```bash
printf 'dist/\n' >> .gitignore
```

- [ ] **Step 2: Перенести страницу в src и вынести текст**

```bash
mkdir -p src i18n && git mv index.html src/index.html
```

Затем править `src/index.html`, секция за секцией сверху вниз, вынося каждую переводимую строку в `i18n/en.json`. Правила:

- **Ключ именуется по месту в странице:** `nav.how`, `hero.title`, `hero.lede`, `trust.upload.dt`, `shoot.callout`, `wedge.h`, `ask.build.p`, `colortype.point2`, `shop.note3.h`, `manifesto.server.p2`, `limits.backup.p`, `faq.cost.q`, `faq.cost.a`, `finale.h`, `footer.note`. Шаблон должен читаться как оглавление страницы.
- **Значение хранится с разметкой:** `<em>`, `<strong>`, `&nbsp;·&nbsp;`, типографские кавычки — всё внутрь строки, дословно.
- **Атрибуты выносятся тоже:** каждый `alt` (их 10), `placeholder`, `aria-label` у `<nav>` и у полосы доверия, `<title>`, `<meta name="description">`, текст `<label class="sr-only">`.
- **Разметка, классы и инлайновые `style` не трогаются.** Выносится только текст.
- **Ключи с префиксом `quote.`** — для строк, которые цитируют интерфейс приложения: `quote.reason1`–`quote.reason4` (`src/index.html:186-189`), `quote.verdict.yes`, `quote.verdict.maybe`, `quote.verdict.no` (`src/index.html:388-390`). Префикс — сигнал переводчику: брать из локали приложения, не переводить.
- **Пути получают `{{@base}}`:** `href="{{@base}}style.css"`, `src="{{@base}}assets/shots/pairs.jpg"`, `srcset="{{@base}}assets/shots/pairs.webp"`, `src="{{@base}}waitlist.js"`.
- **`<html lang="en">` пока не трогать** — служебные ключи `@lang`, `@canonical`, `@alternates`, `@langswitch` подставляются в Task 5. Здесь их в шаблоне быть не должно, иначе не пройдёт проверка совпадения.

Пример готового куска:

```html
<h1 class="display hero__title">{{hero.title}}</h1>
<p class="lede hero__lede">{{hero.lede}}</p>
```

```json
{
  "hero.title": "Your whole wardrobe, and a straight answer about it.",
  "hero.lede": "Ecru catalogues what you own, builds outfits from it, and remembers what you wore. Every time it says two things go together, it tells you <em>why</em> — in one sentence."
}
```

- [ ] **Step 3: Собрать**

Run: `node build.js`
Expected: сборка падает, пока в `i18n/en.json` нет ключа для каждого `{{…}}` шаблона, и называет недостающие. Дописывать, пока не выведет `зібрано: 1 сторінок` (в `LANGS` пока только `en` — временно убрать `'uk'` из константы на время этой задачи, вернуть в Task 8).

- [ ] **Step 4: Написать тест на точное совпадение с прежней страницей**

Создать `tests/output.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const { execFileSync } = require('node:child_process');

const ROOT = path.join(__dirname, '..');

test('зібрана англійська сторінка не втратила жодного символа', () => {
  execFileSync('node', ['build.js'], { cwd: ROOT });
  const built = fs.readFileSync(path.join(ROOT, 'dist', 'index.html'), 'utf8');
  const original = execFileSync('git', ['show', 'HEAD:index.html'], {
    cwd: ROOT,
    encoding: 'utf8',
    maxBuffer: 1024 * 1024,
  });
  assert.strictEqual(built, original);
});
```

- [ ] **Step 5: Запустить и добиться совпадения**

Run: `node --test tests/output.test.js`
Expected: PASS.

Если тест падает, `assert.strictEqual` покажет место расхождения — это либо потерянный при переносе символ, либо изменившийся пробел. Быстрее всего смотреть так:

```bash
diff <(git show HEAD:index.html) dist/index.html
```

Расхождений быть не должно ни одного: подстановка чисто текстовая, а `@base` для `en` пуст.

- [ ] **Step 6: Прогнать весь набор тестов**

Run: `node --test tests/`
Expected: PASS, 26 tests

- [ ] **Step 7: Коммит**

```bash
git add -A src i18n build.js tests .gitignore
git commit -m "refactor: сторінка стала шаблоном, англійський текст — словником

Зібрана сторінка збігається з попередньою символ у символ, тест тримає
цю властивість."
```

**Важно:** после этого коммита тест из Step 4 перестаёт быть осмысленным — `HEAD:index.html` больше не существует. Он удаляется в Task 5, отработав ровно один раз как приёмка переноса.

---

### Task 5: Метаданные, hreflang и переключатель языков

**Files:**
- Modify: `src/index.html`
- Modify: `i18n/en.json`
- Modify: `style.css`
- Delete: `tests/output.test.js`
- Create: `tests/page.test.js`

**Interfaces:**
- Consumes: `metaFor` из Task 2, `build` из Task 3
- Produces: страница со служебными ключами и переключателем; класс `.nav__langs` и `.site-footer__langs` в CSS

- [ ] **Step 1: Удалить отработавший тест переноса**

```bash
git rm tests/output.test.js
```

- [ ] **Step 2: Написать падающий тест на собранную страницу**

Создать `tests/page.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const { execFileSync } = require('node:child_process');

const ROOT = path.join(__dirname, '..');
const DIST = path.join(ROOT, 'dist');

test.before(() => execFileSync('node', ['build.js'], { cwd: ROOT }));

function page(rel) {
  return fs.readFileSync(path.join(DIST, rel), 'utf8');
}

test('кожна сторінка оголошує свою мову', () => {
  assert.match(page('index.html'), /<html lang="en">/);
});

test('кожна сторінка вказує на себе канонічним посиланням', () => {
  assert.match(page('index.html'), /rel="canonical" href="https:\/\/vladarey\.github\.io\/ecru-landing\/"/);
});

test('сторінка перелічує всі мови у hreflang і додає x-default', () => {
  const html = page('index.html');
  assert.match(html, /hreflang="en"/);
  assert.match(html, /hreflang="x-default"/);
});

test('перемикач мов є в шапці і в підвалі', () => {
  const html = page('index.html');
  assert.strictEqual(html.match(/aria-current="page"/g).length, 2);
});

test('у зібраній сторінці не лишилось незамінених ключів', () => {
  assert.doesNotMatch(page('index.html'), /\{\{/);
});
```

- [ ] **Step 3: Запустить и убедиться, что падает**

Run: `node --test tests/page.test.js`
Expected: FAIL — `<html lang="en">` есть, но `canonical` и `aria-current` отсутствуют

- [ ] **Step 4: Подставить служебные ключи в шаблон**

В `src/index.html` заменить `<html lang="en">` на `<html lang="{{@lang}}">` и дописать в `<head>` после `<title>`:

```html
    <link rel="canonical" href="{{@canonical}}" />
    {{@alternates}}
```

- [ ] **Step 5: Вставить переключатель в шапку и в подвал**

В `src/index.html`, внутри `<nav class="nav__links">`, после ссылки `.nav__cta`:

```html
          <span class="nav__langs">{{@langswitch}}</span>
```

В подвале, внутри `.site-footer__meta`, первым элементом:

```html
          <span class="site-footer__langs">{{@langswitch}}</span>
```

Подвал обязателен: `style.css:…` прячет ссылки навигации до `64rem` (`.nav__links a:not(.nav__cta) { display: none }`), поэтому на телефоне шапка переключатель не покажет. Подвал — единственное место, где он есть на всех ширинах, и это осознанно.

- [ ] **Step 6: Стилизовать переключатель**

Дописать в `style.css` после блока `.nav__cta`:

```css
/* Перемикача мов немає в жодному артборді канваса — це третій свідомий
   відступ від макета. Тримаємо його максимально тихим: моно, дрібно, тим
   самим кольором, що й посилання навігації. */
.nav__langs,
.site-footer__langs {
  display: inline-flex;
  align-items: baseline;
  gap: 0.5rem;
  font-family: var(--mono);
  font-size: 0.8125rem;
  letter-spacing: 0.02em;
}

.nav__langs { display: none; }

.nav__langs a,
.site-footer__langs a { color: var(--prose); }

.nav__langs a:hover,
.site-footer__langs a:hover { color: var(--accent); }

.nav__langs [aria-current],
.site-footer__langs [aria-current] { color: var(--ink); }

@media (min-width: 64rem) {
  .nav__langs { display: inline-flex; }
}
```

- [ ] **Step 7: Собрать и прогнать тесты**

Run: `node build.js && node --test tests/`
Expected: PASS, 30 tests

- [ ] **Step 8: Посмотреть глазами**

```bash
open dist/index.html
```

Проверить: переключатель в шапке на широком окне и в подвале на узком, «English» не является ссылкой, вёрстка шапки не поехала.

- [ ] **Step 9: Коммит**

```bash
git add -A src i18n style.css tests
git commit -m "feat: canonical, hreflang і перемикач мов на сторінці"
```

---

### Task 6: Сообщения формы

**Files:**
- Modify: `src/index.html`
- Modify: `i18n/en.json`
- Modify: `waitlist.js`
- Create: `tests/waitlist.test.js`

**Interfaces:**
- Consumes: ничего нового
- Produces: ключи `form.invalid`, `form.unwired`, `form.sending`, `form.ok`, `form.fail`

- [ ] **Step 1: Написать падающий тест**

Создать `tests/waitlist.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');

const ROOT = path.join(__dirname, '..');

test('у waitlist.js не лишилось жодного тексту для людини', () => {
  const js = fs.readFileSync(path.join(ROOT, 'waitlist.js'), 'utf8');
  const code = js.replace(/^\s*\/\/.*$/gm, '');
  const strings = code.match(/(['"])(?:(?!\1)[^\\]|\\.)*\1/g) || [];
  const prose = strings.filter((s) => /\s\w+\s/.test(s) && !/^['"][.#\[]/.test(s));
  assert.deepStrictEqual(prose, [], `лишились літерали: ${prose.join(', ')}`);
});

test('обидві форми несуть усі п’ять повідомлень', () => {
  const html = fs.readFileSync(path.join(ROOT, 'dist', 'index.html'), 'utf8');
  for (const attr of ['invalid', 'unwired', 'sending', 'ok', 'fail']) {
    const found = html.match(new RegExp(`data-msg-${attr}="[^"]+"`, 'g')) || [];
    assert.strictEqual(found.length, 2, `data-msg-${attr} має бути на обох формах`);
  }
});
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node build.js && node --test tests/waitlist.test.js`
Expected: FAIL — в `waitlist.js` найдены литералы

- [ ] **Step 3: Добавить атрибуты обеим формам**

В `src/index.html` обе `<form class="waitlist">` (герой и финал) получают:

```html
      <form class="waitlist" data-endpoint=""
            data-msg-invalid="{{form.invalid}}"
            data-msg-unwired="{{form.unwired}}"
            data-msg-sending="{{form.sending}}"
            data-msg-ok="{{form.ok}}"
            data-msg-fail="{{form.fail}}" novalidate>
```

В `i18n/en.json` — существующий текст дословно:

```json
{
  "form.invalid": "That does not look like an email address.",
  "form.unwired": "The waitlist is not connected yet, so nothing was sent. Check back shortly.",
  "form.sending": "Sending…",
  "form.ok": "You are on the list. No other mail, ever.",
  "form.fail": "That did not go through. Try again in a moment."
}
```

- [ ] **Step 4: Заменить литералы в waitlist.js**

```js
// Форма чекає на endpoint. Поки його немає, вона про це чесно каже —
// мовчазний «Thanks!» без жодного запиту був би брехнею відвідувачу.
//
// Щоб підключити: постав URL у data-endpoint у src/index.html. Більше нічого.
//
// Тексти живуть у data-msg-* поруч із data-endpoint, а не тут: так усі
// рядки сторінки лишаються в одному місці — i18n/<lang>.json — і скрипт
// не знає, якою мовою його відкрили.
document.querySelectorAll('.waitlist').forEach((form) => {
  const note = form.querySelector('.form-note');
  const input = form.querySelector('input[type="email"]');
  const button = form.querySelector('button');
  const msg = form.dataset;

  form.addEventListener('submit', async (event) => {
    event.preventDefault();

    if (!input.value.trim() || !input.checkValidity()) {
      note.textContent = msg.msgInvalid;
      return;
    }

    const endpoint = msg.endpoint;

    if (!endpoint) {
      note.textContent = msg.msgUnwired;
      return;
    }

    button.disabled = true;
    note.textContent = msg.msgSending;

    try {
      const response = await fetch(endpoint, {
        method: 'POST',
        headers: { Accept: 'application/json' },
        body: new FormData(form),
      });

      if (!response.ok) throw new Error(String(response.status));

      form.reset();
      note.textContent = msg.msgOk;
    } catch {
      note.textContent = msg.msgFail;
    } finally {
      button.disabled = false;
    }
  });
});
```

- [ ] **Step 5: Собрать и прогнать тесты**

Run: `node build.js && node --test tests/`
Expected: PASS, 32 tests

- [ ] **Step 6: Проверить форму руками**

```bash
open dist/index.html
```

Отправить пустую форму → сообщение о невалидном адресе. Ввести `a@b.co` → сообщение о неподключённом endpoint. Оба текста должны совпадать с прежними дословно.

- [ ] **Step 7: Коммит**

```bash
git add -A src i18n waitlist.js tests
git commit -m "refactor: тексти форми переїхали в data-msg-*, скрипт більше не знає мови"
```

---

### Task 7: Кириллица в заголовочном шрифте

Без этой задачи украинскую страницу выпускать нельзя: `style.css:110` объявляет Instrument Serif только в `latin` и `latin-ext`, у гарнитуры кириллицы нет вовсе, и все заголовки на `/uk/` упали бы в `Georgia`.

**Files:**
- Create: `assets/fonts/ibm-plex-serif-400-cyrillic.woff2`
- Modify: `style.css:110-131` (блок `@font-face` для Instrument Serif) и `style.css:174` (токен `--display`)
- Create: `tests/fonts.test.js`

**Interfaces:**
- Consumes: ничего
- Produces: семейство `'Ecru Display Cyrillic'` в стеке `--display`

- [ ] **Step 1: Скачать кириллический сабсет IBM Plex Serif**

Берём ровно тот же нарезанный Google Fonts файл, из которого сделаны остальные шрифты в `assets/fonts` — так сабсеты и `unicode-range` остаются согласованными:

```bash
curl -sS -H 'User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120 Safari/537.36' \
  'https://fonts.googleapis.com/css2?family=IBM+Plex+Serif:wght@400' \
  -o /tmp/plex-serif.css
grep -B3 'U+0400-045F' /tmp/plex-serif.css
```

Найти в выводе блок с `unicode-range`, начинающимся с `U+0301, U+0400-045F` — это кириллический сабсет. Скачать его `src`:

```bash
curl -sS -o assets/fonts/ibm-plex-serif-400-cyrillic.woff2 '<URL из блока>'
ls -l assets/fonts/ibm-plex-serif-400-cyrillic.woff2
```

Ожидаемо: файл порядка 15–25 КБ. Если меньше килобайта — скачалась страница ошибки, не шрифт.

Лицензия уже покрыта лежащим `assets/fonts/ibm-plex-OFL.txt`: Plex Serif под тем же OFL, что Sans и Mono. Отдельный файл лицензии не нужен.

- [ ] **Step 2: Написать падающий тест**

Создать `tests/fonts.test.js`:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');

const ROOT = path.join(__dirname, '..');
const css = fs.readFileSync(path.join(ROOT, 'style.css'), 'utf8');

test('кожен @font-face вказує на файл, який справді лежить у репозиторії', () => {
  const urls = Array.from(css.matchAll(/url\('([^']+)'\)/g), (m) => m[1]);
  assert.ok(urls.length > 0);
  for (const url of urls) {
    assert.ok(fs.existsSync(path.join(ROOT, url)), `немає файла ${url}`);
  }
});

test('заголовковий стек має гарнітуру для кирилиці', () => {
  const display = css.match(/--display:\s*([^;]+);/)[1];
  assert.match(display, /Ecru Display Cyrillic/);
});

test('кирилична гарнітура обмежена кириличним діапазоном', () => {
  const face = css.match(/@font-face\s*\{[^}]*Ecru Display Cyrillic[^}]*\}/)[0];
  assert.match(face, /U\+0400-045F/);
  assert.match(face, /ibm-plex-serif-400-cyrillic\.woff2/);
});

test('Instrument Serif лишився без кирилиці — латиниця не змінилась', () => {
  const faces = css.match(/@font-face\s*\{[^}]*Instrument Serif[^}]*\}/g);
  assert.strictEqual(faces.length, 2);
  for (const face of faces) {
    assert.doesNotMatch(face, /U\+0400-045F/);
  }
});
```

- [ ] **Step 3: Запустить и убедиться, что падает**

Run: `node --test tests/fonts.test.js`
Expected: FAIL — в `--display` нет `Ecru Display Cyrillic`

- [ ] **Step 4: Подключить шрифт**

В `style.css` после второго блока Instrument Serif (после строки 131) дописать:

```css
/* У Instrument Serif кирилиці немає зовсім — не підмножини бракує, а самих
   гліфів. Без цього всі заголовки українською тихо падали б у Georgia, і
   титульне обличчя сторінки на /uk/ було б чужим. Беремо IBM Plex Serif:
   родина Plex уже на сторінці, тож шрифтова система лишається однією.
   Діапазон — тільки кирилиця, тому en, pl і de не змінюються ні на піксель. */
@font-face {
  font-family: 'Ecru Display Cyrillic';
  font-style: normal;
  font-weight: 400;
  font-display: swap;
  src: url('assets/fonts/ibm-plex-serif-400-cyrillic.woff2') format('woff2');
  unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
}
```

И в токене на строке 174:

```css
  --display: 'Instrument Serif', 'Ecru Display Cyrillic', 'Iowan Old Style', Georgia, serif;
```

- [ ] **Step 5: Прогнать тесты**

Run: `node --test tests/`
Expected: PASS, 36 tests

- [ ] **Step 6: Коммит**

```bash
git add assets/fonts/ibm-plex-serif-400-cyrillic.woff2 style.css tests/fonts.test.js
git commit -m "feat: кирилична заголовкова гарнітура — IBM Plex Serif за unicode-range"
```

---

### Task 8: Украинский язык

**Files:**
- Create: `i18n/uk.json`
- Modify: `build.js` (вернуть `'uk'` в `LANGS`)
- Modify: `style.css` (правка типографики `:lang(uk)`)
- Modify: `tests/page.test.js`

**Interfaces:**
- Consumes: всё предыдущее
- Produces: `dist/uk/index.html`

- [ ] **Step 1: Вернуть украинский в список языков**

В `build.js`: `const LANGS = ['en', 'uk'];`

- [ ] **Step 2: Получить список ключей для перевода**

```bash
node -e "const t=require('fs').readFileSync('src/index.html','utf8');const {keysIn}=require('./build.js');console.log([...keysIn(t)].filter(k=>!k.startsWith('@')).sort().join('\n'))" > /tmp/keys.txt
wc -l /tmp/keys.txt
```

- [ ] **Step 3: Перевести**

Создать `i18n/uk.json` со всеми ключами из `/tmp/keys.txt`. Правила:

- **Ключи `quote.*` не переводить.** Это дословные цитаты интерфейса приложения — объяснения пар, вердикты примерочной. Взять действующие украинские строки из локали приложения. Если её сейчас нет под рукой — остановиться и спросить, а не переводить с английского: иначе страница покажет фразы, которых в украинском приложении не существует.
- **Кавычки — « »,** не английские “ ”. В JSON они экранирования не требуют.
- **Разметку внутри значений сохранять:** `<em>`, `<strong>`, `&nbsp;·&nbsp;` остаются на своих смысловых местах.
- **Ключ `trust.langs.dd`** перечисляет языки приложения — «Ukrainian, English, Polish, Spanish». Это языки приложения, не сайта; на украинской странице те же четыре, названные по-украински.
- **Тире и неразрывные пробелы** держать как в оригинале: страница набрана длинным тире `—`.

- [ ] **Step 4: Собрать**

Run: `node build.js`
Expected: `зібрано: 2 сторінок`. Пока хоть один ключ не переведён, сборка падает и называет все недостающие сразу.

- [ ] **Step 5: Расширить тесты на вторую страницу**

Дописать в `tests/page.test.js`:

```js
test('українська сторінка зібралась і оголошує свою мову', () => {
  assert.match(page('uk/index.html'), /<html lang="uk">/);
});

test('шляхи на українській сторінці піднімаються на рівень', () => {
  const uk = page('uk/index.html');
  assert.match(uk, /href="\.\.\/style\.css"/);
  assert.match(uk, /src="\.\.\/assets\/shots\/pairs\.jpg"/);
  assert.doesNotMatch(uk, /src="assets\//);
});

test('українська сторінка вказує канонічним посиланням на себе', () => {
  assert.match(page('uk/index.html'), /rel="canonical" href="[^"]*\/uk\/"/);
});

test('обидві сторінки посилаються одна на одну через hreflang', () => {
  for (const rel of ['index.html', 'uk/index.html']) {
    assert.match(page(rel), /hreflang="uk"/);
    assert.match(page(rel), /hreflang="en"/);
  }
});

test('у жодній зібраній сторінці не лишилось ключів', () => {
  for (const rel of ['index.html', 'uk/index.html']) {
    assert.doesNotMatch(page(rel), /\{\{/, rel);
  }
});

test('українська сторінка не містить англійського тексту героя', () => {
  assert.doesNotMatch(page('uk/index.html'), /Your whole wardrobe/);
});
```

- [ ] **Step 6: Прогнать тесты**

Run: `node --test tests/`
Expected: PASS, 42 tests

- [ ] **Step 7: Посмотреть глазами на обеих ширинах**

```bash
open dist/uk/index.html
```

Проверить в окне 1440 и 390 (артборды канваса):

1. Заголовки набраны IBM Plex Serif, а не Georgia. Проверить в DevTools: Inspect на `h1` → Computed → Rendered Fonts должно показать `IBM Plex Serif`.
2. `h1` героя и `h2` секций не переполняют колонку и не разваливаются на некрасивое число строк.
3. Полоса доверия из четырёх колонок не ломается на более длинных украинских подписях.
4. Кнопка формы вмещает украинский текст.
5. Переключатель в шапке (1440) и в подвале (390) показывает «Українська» не ссылкой.
6. Все десять скриншотов на месте — это проверка `{{@base}}`.
7. Кавычки в тексте — « », а не английские “ ”. Проверять по всей странице, а
   не только в заголовках: кавычки есть в карточке причин, в заголовке секции
   про календарь и в FAQ.
8. Форма показывает украинские сообщения. Отправить пустую → сообщение о
   невалидном адресе; ввести `a@b.co` → сообщение о неподключённом endpoint.

- [ ] **Step 8: Поправить типографику заголовков под Plex Serif**

`line-height: 1.06` и `letter-spacing: -0.01em` в `.display` (`style.css:295`) подобраны под Instrument Serif. Plex Serif шире, ниже по контрасту и с бо́льшим x-height, поэтому на том же кегле выглядит крупнее и плотнее.

Величина поправки определяется по результату Step 7, а не заранее. Если заголовки читаются как более тяжёлые или переполняют строку, дописать в `style.css` после `.h-wide`:

```css
/* Plex Serif ширший за Instrument Serif і має більший x-height, тож на тому
   самому кеглі виглядає крупніше. Поправка тільки для української —
   латинські сторінки лишаються такими, як у канвасі. */
:lang(uk) .display {
  letter-spacing: 0;
  line-height: 1.1;
}
```

Если по Step 7 поправка не нужна — не добавлять её. Пустое правило хуже отсутствующего.

- [ ] **Step 9: Коммит**

```bash
git add -A i18n build.js style.css tests
git commit -m "feat: українська версія сторінки"
```

---

### Task 9: Деплой и документация

Последняя задача. До неё в `main` ничего не уходит.

**Files:**
- Modify: `.github/workflows/` (файл деплоя)
- Modify: `README.md`

**Interfaces:**
- Consumes: `node build.js` из Task 3
- Produces: пайплайн, публикующий `dist/`

- [ ] **Step 1: Добавить шаг сборки в workflow**

В файле деплоя заменить комментарий и шаг загрузки:

```yaml
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      # Сайт більше не є тим, що лежить у репозиторії: сторінка на кожну мову
      # збирається з src/index.html та i18n/*.json. setup-node не потрібен —
      # на ubuntu-latest Node уже є, а залежностей у build.js немає.
      - run: node build.js
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
```

- [ ] **Step 2: Проверить, что собирается на чистой копии**

```bash
git stash -u 2>/dev/null; rm -rf /tmp/ecru-check && git clone -q . /tmp/ecru-check && (cd /tmp/ecru-check && node build.js && ls dist dist/uk)
```

Expected: `зібрано: 2 сторінок`, в `dist` — `index.html`, `style.css`, `waitlist.js`, `assets`, `.nojekyll`, `uk`; в `dist/uk` — `index.html`.

- [ ] **Step 3: Обновить README**

Правки:

- Заменить «Plain HTML and CSS. No build step: open `index.html` in a browser.» на:

      Plain HTML and CSS, one page per language. `node build.js` renders
      `src/index.html` against each `i18n/<lang>.json` into `dist/` — no
      dependencies, nothing to install. Open `dist/index.html` in a browser;
      paths are relative, so `dist/uk/index.html` works over `file://` too.
      Pushing to `main` builds and deploys to
      https://vladarey.github.io/ecru-landing/
- Описать `src/index.html`, `i18n/<lang>.json` и порядок добавления языка: дописать код в `LANGS` в `build.js`, положить `i18n/<code>.json`, добавить endonym в `LANG_NAMES`.
- Добавить в список сознательных отступлений от канваса третий пункт — переключатель языков, которого нет ни на одном артборде.
- Записать правило про `quote.*`: эти строки цитируют интерфейс приложения и берутся из его локали, а не переводятся.
- В разделе про шрифты упомянуть `ibm-plex-serif-400-cyrillic.woff2` и объяснить, зачем он: у Instrument Serif нет кириллицы, поэтому заголовки на `/uk/` идут другой гарнитурой.
- Поправить инструкцию про `data-endpoint`: атрибут теперь в `src/index.html`, обе формы по-прежнему нужно заполнить.

- [ ] **Step 4: Прогнать весь набор тестов**

Run: `node --test tests/`
Expected: PASS, 42 tests

- [ ] **Step 5: Коммит**

```bash
git add .github README.md
git commit -m "ci: збірка перед публікацією, dist замість кореня репозиторію"
```

- [ ] **Step 6: Слить в main и проследить деплой**

```bash
git checkout main && git merge --no-ff i18n && git push
gh run watch
```

- [ ] **Step 7: Проверить живой сайт**

Открыть `https://vladarey.github.io/ecru-landing/` и `https://vladarey.github.io/ecru-landing/uk/`. Проверить: обе страницы отдаются, стили и шрифты подхватились, скриншоты на месте, переключатель работает в обе стороны, в DevTools → Network нет ни одного запроса на внешний домен.

---

## Что этот план сознательно не делает

- **Не сохраняет якорь при переключении языка.** Со `#limits` на `/uk/` человек попадёт в начало английской страницы. Три строки JS — но это первый шаг к роутеру на статической странице. Добавляем, если кто-то пожалуется.
- **Не редиректит по `navigator.language`.** Ломает индексацию и запирает пришедшего по прямой ссылке.
- **Не добавляет `pl` и `de`.** Инфраструктура к ним готова: словарь, строка в `LANGS`, endonym. Но перед `de` надо решить открытый вопрос спеки — полоса доверия обещает приложение на украинском, английском, польском и **испанском**, а немецкого в этом списке нет.
- **Не заводит `package.json`.** Тесты идут через встроенный `node --test`, зависимостей нет.
